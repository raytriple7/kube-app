apiVersion: v1
kind: Service
metadata:
  name: vprocache01
  namespace: dev # Include the namespace if needed
spec:
  ports:
  - port: 11211
    targetPort: 11211 # Changed to the actual port number
    protocol: TCP
  selector:
    app: vpromc
  type: ClusterIP

